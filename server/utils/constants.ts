
const DEFAULT_SIGN = '|';
const DEFAULT_TITLE_SIGN = '*';

const INVALID_PROXY = 'INVALID_PROXY';

const DATE_FORMAT = {
  'shortInt': 'YYMMDDHHmmss',
  'standard': 'YYYY-MM-DD HH:mm:ss',
}

const LOG_CONTEXT_KEYS = {
  file: 'file',
  operate: 'operate',
}

export {
  DEFAULT_SIGN,
  DEFAULT_TITLE_SIGN,
  DATE_FORMAT,
  INVALID_PROXY,
  LOG_CONTEXT_KEYS,
}