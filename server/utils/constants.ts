
const DEFAULT_SIGN = '|';
const DEFAULT_TITLE_SIGN = '*';

const DATE_FORMAT = {
  'shortInt': 'YYMMDDHHmmss',
  'standard': 'YYYY-MM-DD HH:mm:ss',
}

const LOG_CONTEXT_KEYS = {
  file: 'file',
  operate: 'operate',
}

export {
  DEFAULT_SIGN,
  DEFAULT_TITLE_SIGN,
  DATE_FORMAT,
  LOG_CONTEXT_KEYS,
}